const express = require('express');
const router = express.Router();

// Define payment/subscription-related routes here later (e.g., create checkout session, handle webhooks)
// router.post('/create-checkout-session', (req, res) => res.send('Payment route working!'));

module.exports = router;